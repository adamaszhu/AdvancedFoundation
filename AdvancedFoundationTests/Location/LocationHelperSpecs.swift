class LocationHelperSpecs: QuickSpec {
    
    override func spec() {
        // COMMENT: LocationHelper is highly related to hardware, so it should be tested manually.
    }
    
}

import Quick
import Nimble
@testable import AdvancedFoundation
